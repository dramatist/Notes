### Annotation

* @Controller、@RestController

* @ControllerAdvice、@RestControllerAdvice
* @RequestMapping
    * @GetMapping
    * @PostMapping
    * @DeleteMapping
    * @PutMapping
    * @PatchMapping

@EnableWebMvc

* DelegatingWebMvcConfiguration：探测并代理WebMvcConfigurer实现类来自定义配置

#### URI  pattern

PathPattern：

AntPathMatcher：5.3版本之前唯一实现，之后的默认实现